{
  "name": "love-language-music",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "pages:dev": "npm run build && npx wrangler@latest pages dev",
    "deploy": "npm run build && npx wrangler@latest pages deploy"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^7.1.6",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0"
  }
}
